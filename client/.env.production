# Vite inlines VITE_* vars at BUILD TIME. Set the real value as a build-time
# environment variable on the Railway client service (Settings → Variables) —
# it will override this placeholder. Point it at the public URL of the
# "server" Railway service, e.g. https://bid-server-production.up.railway.app
VITE_BACKEND_URL=
