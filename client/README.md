# Topbrass Frontend

React + Vite frontend for the Topbrass role-based job application platform.

## Environment

- Local development uses `client/.env.development` or `client/.env.local`
- Production builds must set `VITE_BACKEND_URL` to the public Render backend URL
